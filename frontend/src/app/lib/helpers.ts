// "use client";
// export const getAccessToken = () => {
//   const tokenObject = JSON.parse(localStorage.getItem("token") as string); // Parse the token object from localStorage
//   const accessToken = tokenObject.accessToken; // Access the accessToken property from the token object
//   return accessToken;
// };
